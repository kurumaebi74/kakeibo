<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div id="wrapper">
  <header>
    <h2>家計簿</h2>
  </header>
  <nav>
    <ul class="main_menu">
      <li><%= link_to("支出登録","/post/new")%></li>
      <li><%= link_to("当月支出", "/") %></li>
      <li><%= link_to("過去6か月間支出","/past")%></li>
      <li><%= link_to("設定","/setting")%></li>
    </ul>
  </nav>
</div>
  <% if flash[:notice] %>
    <div class="flash">
      <%= flash[:notice] %>
    </div>
  <% end %>
    <%= yield %>
  </body>
</html>
